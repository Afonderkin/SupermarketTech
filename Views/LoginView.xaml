<Window x:Class="SupermarketTech.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход - Супермаркет" Height="300" Width="400" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <viewModels:LoginViewModel xmlns:viewModels="clr-namespace:SupermarketTech.ViewModels"/>
    </Window.DataContext>
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Логин" />
        <TextBox Grid.Row="1" Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10"/>

        <PasswordBox Grid.Row="2" x:Name="PwdBox" Margin="0,5,0,10"/>
        <Button Grid.Row="3" Content="Войти" Height="32" Width="100" HorizontalAlignment="Left"
                Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PwdBox}" />

        <TextBlock Grid.Row="3" Text="{Binding Error}" Foreground="Red" HorizontalAlignment="Right" VerticalAlignment="Center"/>
    </Grid>
</Window>