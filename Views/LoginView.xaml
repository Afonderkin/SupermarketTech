<Window x:Class="SupermarketTech.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:SupermarketTech.ViewModels"
        Background="{StaticResource WindowBackgroundGradient}"
        Title="Вход - Супермаркет" Height="360" Width="400" WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Window.DataContext>
        <viewModels:LoginViewModel />
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Text="Вход в Супермаркет" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <!-- Логин -->
        <TextBlock Grid.Row="1" Text="Логин" />
        <TextBox Grid.Row="1" Margin="0,20,0,0"
                 Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" />

        <!-- Пароль -->
        <TextBlock Grid.Row="2" Text="Пароль" Margin="0,10,0,0"/>
        <PasswordBox Grid.Row="2" x:Name="PwdBox" Margin="0,30,0,0"/>

        <!-- Кнопка Войти и сообщение об ошибке -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,20,0,0" HorizontalAlignment="Left">
            <Button Content="Войти" Height="32" Width="100"
                    Command="{Binding LoginCommand}" 
                    CommandParameter="{Binding ElementName=PwdBox}" />
            <TextBlock Text="{Binding Error}" Foreground="Red" VerticalAlignment="Center" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Ссылка на регистрацию -->
        <TextBlock Grid.Row="4" HorizontalAlignment="Center" Margin="0,30,0,0">
            <Run Text="Нет аккаунта? "/>
            <Run Text="Зарегистрируйтесь" FontWeight="Bold" Foreground="Blue" Cursor="Hand">
                <Run.InputBindings>
                    <MouseBinding MouseAction="LeftClick" Command="{Binding ShowRegisterCommand}" />
                </Run.InputBindings>
            </Run>
        </TextBlock>
    </Grid>
</Window>
