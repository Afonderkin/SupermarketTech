<Window x:Class="SupermarketTech.Views.RegistrationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:SupermarketTech.ViewModels"
        Background="{StaticResource WindowBackgroundGradient}"
        Title="Регистрация" Height="360" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">

    <Window.DataContext>
        <viewModels:RegistrationViewModel />
    </Window.DataContext>

    <DockPanel Margin="20" LastChildFill="False">

        <!-- Заголовок вверху -->
        <TextBlock DockPanel.Dock="Top" 
                   Text="Регистрация" 
                   FontSize="18" 
                   FontWeight="Bold" 
                   HorizontalAlignment="Center" 
                   Margin="0,0,0,20"/>

        <!-- Основной контент по центру -->
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,20">
            <!-- Логин -->
            <StackPanel Margin="0,0,0,10">
                <TextBlock Text="Логин" Margin="0,0,0,2"/>
                <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}" 
                         Height="32"/>
            </StackPanel>

            <!-- Пароль -->
            <StackPanel>
                <TextBlock Text="Пароль" Margin="0,0,0,2"/>
                <PasswordBox x:Name="PwdBox" Height="32"/>
            </StackPanel>

            <!-- Кнопки -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,20,0,0">
                <Button Content="Зарегистрироваться"
                        Width="120" Height="32"
                        Command="{Binding RegisterCommand}" 
                        CommandParameter="{Binding ElementName=PwdBox}" 
                        Margin="0,0,10,0"/>
                <Button Content="Отмена"
                        Width="80" Height="32"
                        Click="Cancel_Click"/>
            </StackPanel>
        </StackPanel>

        <!-- Ошибка внизу -->
        <TextBlock DockPanel.Dock="Bottom" 
                   Text="{Binding Error}" 
                   Foreground="Red" 
                   HorizontalAlignment="Left" 
                   Margin="0,10,0,0"/>

    </DockPanel>
</Window>