<UserControl x:Class="SupermarketTech.Views.MyOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer Margin="10">
        <ItemsControl ItemsSource="{Binding Orders}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="#DDD"
                            BorderThickness="1"
                            Padding="10"
                            Margin="0,0,0,12"
                            CornerRadius="6">
                        <StackPanel>

                            <Grid Margin="0,0,0,8">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Заказ -->
                                <TextBlock Text="{Binding OrderId, StringFormat=Заказ №{0}}"
                                           FontWeight="Bold"
                                           VerticalAlignment="Center"/>

                                <!-- Дата и кнопка -->
                                <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding DeliveryDate, StringFormat=Доставка: {0:dd.MM.yyyy}}"
                                               Foreground="#666"
                                               Margin="0,0,0,2"/>
                                    <TextBlock Text="{Binding TotalPrice, StringFormat=Итого: {0:N0} ₽}"
                                               Foreground="#2E8B57"
                                               FontWeight="Bold"
                                               Margin="0,0,0,4"/>
                                    <Button Content="Отменить заказ"
                                            Command="{Binding CancelOrderCommand}"
                                            Background="#FF6347"
                                            Foreground="White"
                                            Padding="4,2"
                                            FontSize="12"/>
                                </StackPanel>
                            </Grid>

                            <!-- Товары -->
                            <ItemsControl ItemsSource="{Binding Items}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border BorderBrush="#EEE"
                                                BorderThickness="1"
                                                Padding="6"
                                                Margin="0,0,0,6">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="70"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>

                                                <Image Source="{Binding Product.ImagePath}"
                                                       Height="60"
                                                       Width="60"
                                                       Stretch="Uniform"/>

                                                <StackPanel Grid.Column="1" Margin="10,0,0,0">
                                                    <TextBlock Text="{Binding Product.Name}"
                                                               FontWeight="Bold"/>

                                                    <TextBlock Text="{Binding Quantity, StringFormat=Кол-во: {0}}" Foreground="#555"/>

                                                    <TextBlock Text="{Binding ShortDescription}"
                                                               TextWrapping="Wrap"
                                                               Foreground="#555"/>

                                                    <TextBlock Text="Подробнее"
                                                               Foreground="#1E90FF"
                                                               Cursor="Hand">
                                                        <TextBlock.InputBindings>
                                                            <MouseBinding MouseAction="LeftClick"
                                                                          Command="{Binding ToggleDescriptionCommand}"/>
                                                        </TextBlock.InputBindings>
                                                    </TextBlock>

                                                    <TextBlock Text="{Binding Product.Description}"
                                                               TextWrapping="Wrap"
                                                               Visibility="{Binding IsExpanded,
                                                               Converter={StaticResource BoolToVisConverter}}"/>
                                                    <TextBlock Text="{Binding Product.Price, StringFormat=Цена: {0:N0} ₽}"
                                                               Foreground="#2E8B57"
                                                               Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ScrollViewer>
</UserControl>
